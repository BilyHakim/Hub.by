[package]
name = "hubby-desktop"
version = "0.1.0"
description = "Hubby desktop application"
authors = ["Hubby"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-localhost = "2"

[features]
custom-protocol = ["tauri/custom-protocol"]
